__pycache__/
*.pyc
*.pyo
.venv/
venv/
node_modules/
.env
# Runtime state is created by deployment/startup and must never be traversed by
# Git. Production deliberately makes this directory inaccessible to deploy.
data/
*.log
.pytest_cache/
.DS_Store

# Server-only TLS private material
deploy/certs/*.pem
deploy/certs/*.key
